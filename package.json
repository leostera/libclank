{
  "name": "libclank",
  "version": "0.1.0",
  "description": "Typed, code-first durable workflows and agent runtime",
  "private": true,
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com/leostera/libclank.git"
  },
  "homepage": "https://github.com/leostera/libclank#readme",
  "type": "module",
  "files": [
    "packages/core/dist",
    "packages/agent/dist",
    "packages/artifacts/dist",
    "packages/scheduler/dist",
    "packages/cloudflare/dist",
    "packages/ui/dist"
  ],
  "exports": {
    "./core": {
      "types": "./packages/core/dist/index.d.ts",
      "default": "./packages/core/dist/index.js"
    },
    "./agent": {
      "types": "./packages/agent/dist/index.d.ts",
      "default": "./packages/agent/dist/index.js"
    },
    "./cloudflare": {
      "types": "./packages/cloudflare/dist/index.d.ts",
      "default": "./packages/cloudflare/dist/index.js"
    },
    "./ui": {
      "types": "./packages/ui/dist/index.d.ts",
      "default": "./packages/ui/dist/index.js"
    }
  },
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run build:workspaces",
    "build:workspaces": "bun run --filter @libclank/core build && bun run --filter @libclank/artifacts build && bun run --filter @libclank/agent build && bun run --filter @libclank/gitlab build && bun run --filter @libclank/scheduler build && bun run --filter @libclank/local build && bun run --filter @libclank/cloudflare build && bun run --filter @libclank/ui build && bun run --filter @libclank/app-agent build && bun run --filter @libclank/app-scheduler build",
    "build:package": "bun run --filter @libclank/core build && bun run --filter @libclank/artifacts build && bun run --filter @libclank/agent build && bun run --filter @libclank/scheduler build && bun run --filter @libclank/cloudflare build && bun run --filter @libclank/ui build",
    "typecheck": "bun run build && bun run typecheck:packages",
    "typecheck:packages": "bun run --filter '*' typecheck",
    "test": "bun run build && bun run test:unit && bun run test:e2e && bun run test:workers",
    "test:unit": "vitest run packages",
    "test:e2e": "vitest run tests/e2e",
    "test:workers": "bun run --filter @libclank/app-scheduler test",
    "example:url-summary": "bun run build && bun examples/local-url-summary/server.ts",
    "example:link-digest": "bun run build && bun examples/link-digest/server.ts",
    "example:parallel-analyses": "bun run build && bun examples/parallel-analyses/server.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "bun run build:package"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^22.10.2",
    "prettier": "^3.9.8",
    "typescript": "^5.7.2",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "@xyflow/react": "^12.11.6",
    "effect": "4.0.0-rc.116",
    "hono": "^4.13.8",
    "react-router": "^8.4.0"
  },
  "peerDependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  }
}
