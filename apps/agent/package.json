{
  "name": "@libclank/app-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@libclank/agent": "workspace:*",
    "agents": "^0.5.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250918.0",
    "wrangler": "^4.34.0"
  }
}
